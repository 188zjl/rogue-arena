# 安全策略

## 私密报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要在公开 Issue 中提交漏洞细节、真实账号、密码、邀请码、Cookie、IP 地址、`.env` 内容、API Key 或运行数据。

报告时可提供受影响版本、复现条件、预期影响和已经去除敏感信息的日志片段。

## 自托管安全清单

- 为每个部署生成独立且不少于 48 个字符的 `SESSION_SECRET`，不要使用 `.env.example` 中的占位值。
- 不要提交 `.env` 或 `data/*.json`；它们已在 `.gitignore` 中排除。
- 公网部署必须使用 HTTPS，并设置 `COOKIE_SECURE=1`。
- 只有位于可信反向代理之后时才设置 `TRUST_PROXY=1`。
- 应用端口默认只绑定 `127.0.0.1`；跨主机反代时使用防火墙仅允许反向代理来源。
- 限制 `.env` 和 `DATA_DIR` 的文件权限，定期备份并测试恢复。
- 村民 AI 默认关闭；启用后，玩家问题与知识库内容会发送给所配置的模型服务。
- 村民 API Key 只能存放在服务端环境变量中，不要写进 HTML、源码、文档、日志或 Issue。

## 凭证泄露处理

如果凭证曾被提交，即使随后删除文件，也应视为已经泄露：立即轮换相关密码、令牌或密钥，然后清理所有可达 Git 历史。只修改最新提交不足以撤销旧提交中的秘密。
